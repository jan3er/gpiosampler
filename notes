c++ version:

install process:
    get sdl dev libs
    sudo apt-get install libsdl1.2-dev libsdl-image1.2-dev libsdl-mixer1.2-dev libsdl-ttf2.0-dev

build:
    g++ -std=c++0x gpiosampler.cpp -lSDL -lSDL_mixer -lwiringPi
